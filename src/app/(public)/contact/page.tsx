import { SectionTitle } from "@/components/ui/SectionTitle";

export default function Page() {
  return (
    <main className="page">
      <div className="container">
        <div className="surface">
          <SectionTitle title="Contact" description="Support en contactstructuur." />
        </div>
      </div>
    </main>
  );
}
