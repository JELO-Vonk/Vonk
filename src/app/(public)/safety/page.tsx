import { SectionTitle } from "@/components/ui/SectionTitle";

export default function Page() {
  return (
    <main className="page">
      <div className="container">
        <div className="surface">
          <SectionTitle title="Veiligheid" description="Reports, blokkeren, verificatie, trust score en moderatie." />
        </div>
      </div>
    </main>
  );
}
