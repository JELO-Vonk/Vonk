import { SectionTitle } from "@/components/ui/SectionTitle";

export default function LiveHistoryPage() {
  return (
    <div className="surface">
      <SectionTitle title="Live geschiedenis" description="Eerdere live sessies, matches en reports." />
    </div>
  );
}
