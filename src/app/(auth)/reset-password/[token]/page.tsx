import { SectionTitle } from "@/components/ui/SectionTitle";

export default function Page() {
  return (
    <div className="surface">
      <SectionTitle title="Tokenpagina" description="Vervang dit scherm later met echte tokenvalidatie." />
    </div>
  );
}
